# Agent error log

- 2026-07-14｜核心结果 JSON/CSV 首次提交被 `git diff --check` 判定为全文件尾随空白｜输出器沿用了 Windows CRLF，CSV writer 还默认使用 `\r\n`｜固定 JSON `newline="\n"` 和 CSV `lineterminator="\n"` 后重新生成｜所有受版本控制的机器产物显式输出 LF 并在提交前执行差异门禁
- 2026-07-14｜五种子检查点审计首次运行无法加载早于 `lag_residual_scale` 的 seed 42 检查点｜新分析入口使用 Lightning 严格加载，却没有复用训练入口已有的受限兼容规则｜改为仅允许缺少零初始化时滞尺度键并拒绝其他不兼容状态｜新增检查点分析入口必须与训练入口共享或等价验证版本兼容规则
- 2026-07-14｜核心结果图测试因把颜色列表整体传给 Matplotlib `errorbar.ecolor` 而失败｜误以为误差棒接口与散点接口一样支持逐点颜色数组｜改为逐点调用 `errorbar` 并保留对应颜色｜多色误差棒必须逐点绘制并由渲染测试覆盖
- 2026-07-14｜搜索实验记录实现时把不存在的 `scripts` 目录一并传给 `rg`，使已有有效匹配仍以非零状态结束｜未先用 `rg --files` 确认仓库顶层目录｜保留已找到的源码位置并停止猜测额外目录｜跨目录搜索前先枚举实际目录，只传入存在的路径
- 2026-07-14｜猜测正式运行目录会保存 `.hydra/overrides.yaml` 并直接读取不存在的路径｜未先枚举该运行目录的实际文件｜改为用 `rg --files` 定位 `hparams.yaml` 并读取有效配置｜读取运行产物前先枚举文件而不假设日志器目录结构
- 2026-07-14｜真实 GPU 冒烟验证在 Rich 进度条收尾时触发 GBK 编码异常｜在非 UTF-8 Windows 捕获终端中保留了交互式进度条｜关闭 `trainer.enable_progress_bar` 后同一训练成功｜自动化训练和捕获终端默认关闭 Rich 进度条
- 2026-07-14｜为规避 Rich 的 GBK 问题设置 `PYTHONUTF8=1`，导致 DeepWater 中本地编码的 editable `.pth` 无法解码｜改变了解释器启动编码而未考虑环境内既有 `.pth`｜撤销该环境变量，仅关闭进度条后成功｜该环境不全局强制 UTF-8，输出编码问题在具体组件层解决
- 2026-07-14｜检查测试进程时再次调用了不在 PATH 中的 `conda`｜忽略了已知且已验证的 DeepWater 绝对解释器路径｜改用 `C:\Program Files\ANACONDA\envs\DeepWater\python.exe` 完成检查和测试｜本项目所有 Python 命令统一复用已验证的绝对解释器路径
- 2026-07-14｜重跑全量测试时把命令超时设成 1 秒并导致 pytest 被终止｜混淆了命令总超时与异步输出让步时间｜改为 120 秒命令超时并取得 `123 passed` 的完整退出码｜测试总超时按历史运行时长留足余量，只有可等待执行单元使用短让步
- 2026-07-13｜用 `Select-Object -First` 直接截断实验 dry-run 的 Python 管道导致命令返回非零｜消费者提前关闭仍在写入 45 条命令的标准输出｜改为完整捕获输出后再显示前几行｜不对长输出进程使用会提前关闭管道的截断操作
- 2026-07-13｜插入实验预设测试时把既有 checkpoint 测试的后半段移动进了新测试｜补丁锚点选在函数中部而未立即检查完整函数边界｜在运行红灯前检查文件尾部并把断言恢复到原测试｜向测试文件中部插入代码后先检查相邻两个完整测试函数再执行
- 2026-07-13｜首次可辨识场景的路由衰减常数使五种子贡献率仅 0.024--0.062｜设计时未在写入固定常数前量化多跳路由方差｜硬门禁阻止训练后，仅用数据指标确定最小通过值 2.0｜任何合成信号强度在模型训练前先跑跨种子贡献率门禁
- 2026-07-13｜数据门禁诊断命令把 `ANACONDA` 路径误写为 `ANACDA`｜手工重复输入绝对路径时漏字符｜改用已验证的 DeepWater 解释器路径重跑｜后续脚本把解释器路径保存为单一变量或复制已验证命令
- 2026-07-13｜根据 PowerShell `Get-Content` 的乱码显示尝试修正 README，补丁因源码不匹配被拒绝｜把终端渲染误当成 UTF-8 文件内容｜用 Python `read_text(encoding="utf-8")` 核对码点后确认源码正确，仅新增所需章节｜判断乱码前先检查文件实际 Unicode 内容
- 2026-07-13｜新增 held-out 报告表格时把 `±` 误写为 `卤`｜补丁文本中的字符未与同文件既有模板核对｜统一改回 `±` 并增加禁止该乱码字符的回归断言｜新增非 ASCII 展示文本时比较同文件工作样例并检查 Unicode 码点
- 2026-07-13｜用 1 秒 shell 超时启动全量 pytest，进程被工具终止｜误以为该调用必然返回可等待的后台 cell｜改用足够的命令超时并完成 68 项测试｜仅对明确返回 cell 的命令使用 wait，普通长测试直接给足超时
- 2026-07-13｜再次直接调用不在 PATH 的 `conda`，随后用递归搜索导致超时｜没有沿用已发现的 DeepWater 绝对解释器路径｜改用 `C:\Program Files\ANACONDA\envs\DeepWater\python.exe` 并成功重跑测试｜本任务后续命令固定使用该绝对路径

- 2026-07-13｜并行环境探测被一个失败命令整体中断｜未隔离预期可能失败的 Git/Conda 子命令｜改为逐项捕获结果并在正确仓库根目录运行｜后续并行探测对每个命令单独处理退出码
- 2026-07-13｜复制根 AGENTS.md 时调用了 V8 环境不存在的 `atob` 和 `TextDecoder`｜错误假设 JavaScript Web API 可用｜改用显式 Base64 与 UTF-8 解码后通过 apply_patch 写入｜使用运行时已声明能力或先做无副作用能力检查
- 2026-07-13｜`conda run` 转发 pytest 输出时发生 GBK 编码异常｜未预设 Windows 中文路径下的 UTF-8 输出｜中间测试直接调用 DeepWater 解释器，最终命令设置 UTF-8 并验证｜Windows 自动化命令显式设置 `PYTHONUTF8=1`
- 2026-07-13｜editable 安装生成的 UTF-8 `.pth` 使 Python 3.10 无法在 GBK 下启动｜仓库中文路径被写入 site-packages 路径文件｜只删除本次生成的 RiverLagNet `.pth`，改用普通安装｜非 ASCII 路径下不使用 editable 安装
- 2026-07-13｜pytest warning 正则令 pyproject.toml 解析失败｜TOML basic string 中反斜杠未转义｜改用 TOML literal string并重跑测试｜正则配置优先使用 literal string
- 2026-07-13｜RuntimeStatsCallback 在 `on_fit_end` 调用 `self.log` 导致成功训练被判失败｜未遵循 Lightning 对回调生命周期的日志限制｜增加 fast-dev 回归测试并改为直接调用 Trainer loggers｜所有自定义回调必须纳入真实 Trainer 生命周期测试
- 2026-07-13｜普通安装后 CLI 找不到 Hydra 根配置｜源码相对路径没有考虑 wheel 内模块位置｜增加 package-data 配置镜像及一致性测试，CLI 改用包内路径｜发布入口必须在非 editable 安装后执行 smoke test
- 2026-07-13｜GPU `16-mixed` 下 attention 切片赋值发生 FP16/FP32 冲突｜CPU 测试未覆盖 softmax 在 autocast 中提升精度的行为｜增加 CUDA autocast 测试，attention 固定 FP32 并在消息乘法边界显式转换｜混合精度模型必须包含真实 CUDA 前向回归测试
- 2026-07-13｜输入变量多于三个时目标标准化维度不匹配｜先裁目标通道再应用全特征 scaler｜增加额外动态变量测试并改为先标准化全特征再取前三个目标｜数据测试必须覆盖 `V>target_dim`
- 2026-07-13｜内联 Python 中直接嵌入中文源路径后被 PowerShell 管道替换为问号｜跨进程脚本文本编码假设错误｜改用环境变量传递 Unicode 路径且脚本标准输入保持 ASCII｜含非 ASCII 的自动化路径统一通过参数或环境变量传递
- 2026-07-13｜数据清单检查用 `Path.read_text()` 默认 GBK 解码 UTF-8 JSON 失败｜遗漏显式文件编码｜改为 `read_text(encoding="utf-8")` 并完成检查｜所有项目 JSON 和 Markdown 读写显式声明 UTF-8
- 2026-07-13｜假设 `conda` 已在 PATH 并用递归搜索定位导致一次超时｜未先读取 Conda 环境注册文件｜从 `.conda/environments.txt` 确认安装位置并使用绝对解释器路径｜Windows 环境发现优先读取注册配置而非递归扫描用户目录
- 2026-07-13｜新导入 CLI 被旧的普通安装遮蔽而首次运行失败｜修改源码后未考虑非 editable 安装不会即时更新｜用当前仓库 `src` 设置 `PYTHONPATH` 后重跑成功｜源码 smoke 在普通安装模式下先重装，或显式绑定当前 `src`
- 2026-07-13｜新增实验账本字段时遗漏 `synthetic_smoke` 配置组｜只修改默认实验配置，未检查同组替代项的结构一致性｜增加真实 Hydra 组合回归测试并同步根配置与包内镜像｜扩展 Hydra 配置结构时遍历同组所有 YAML 并测试替代组合
- 2026-07-13｜首次评估命令的 Windows checkpoint 路径被 Hydra override 解析器拒绝｜只做了 PowerShell 引号，未让引号成为传入 Hydra 的值且文件名包含 `=`｜改用带内嵌引号的相对正斜杠路径并完成四项评估｜Hydra 文件路径 override 必须测试盘符、反斜杠和等号三类特殊字符
- 2026-07-13｜Caravan 链接表日期范围统计因字符串与缺失浮点混合而失败｜直接对未解析的源日期列调用最小值｜显式使用容错日期解析后重跑｜外部 Parquet 日期列先检查 dtype 并统一解析再做时间统计
- 2026-07-13｜首次按 `China` 筛选 Caravan 后对空集最大值转整数失败｜未核对源国家枚举且未防御空分组｜查明规范值为 `People's Republic of China` 并重跑完整统计｜分类过滤前先列出匹配枚举且聚合代码必须处理空集
- 2026-07-13｜诊断时读取不存在的 `configs/experiment/identifiable_v1.yaml` 导致一次命令失败｜把实验套件预设误当成 Hydra 实验配置文件｜立即列出实际配置目录并改从套件代码读取预设｜读取推断路径前先用 `rg --files` 或目录清单确认文件存在
- 2026-07-13｜融合诊断训练的外层命令超时过短，关闭输出管道并产生一条 crash 记录｜把工具让出控制的时间误设成了进程硬超时｜保留 crash 记录并用新实验名、长进程超时重跑｜长训练通过可恢复执行单元让出控制，不用短 `timeout_ms` 模拟异步
- 2026-07-13｜融合 checkpoint 诊断再次被旧的普通安装版本遮蔽｜内联只读脚本遗漏当前仓库 `src` 绑定，重复触发已知的非 editable 安装问题｜补充 `PYTHONPATH=src` 后重跑并计划正式训练前重新安装｜凡修改模型结构后，所有 Python 子进程统一先绑定当前源码或重装再执行
- 2026-07-13｜查找配置测试时又尝试读取不存在的 `tests/test_config_files.py`｜在 `rg` 已定位可用测试后仍追加了推测路径｜改在已存在的 DataModule Hydra 组合测试中增加断言｜文件查找结果已足够时不再尝试未确认的候选路径
- 2026-07-13｜最终汇总在 Windows 上生成 CRLF，`git diff --check` 将新增行判为尾随空白｜报告写入器依赖平台默认换行且此前测试只检查内容｜改为显式 LF 写入并增加字节级回归断言｜所有受版本控制的生成文本同时验证内容、UTF-8 和 LF 换行
- 2026-07-14｜真实数据筛选误用重命名后的河段 ID，导致 Polars 类型错误｜未在重命名后区分源站点 ID 与模型节点 ID｜改用 `source_station_id` 并由端到端小型真实数据夹具回归验证｜涉及站点-河段映射时始终保留语义明确的双 ID 字段
- 2026-07-14｜首次运行新增真实数据 CLI 时被旧的普通安装遮蔽｜修改源码后未先核对 `DeepWater` 中 RiverLagNet 的安装模式｜重新安装当前仓库并完成 CLI 与全量测试｜新增模块 smoke 前先执行 `pip show` 并绑定当前源码版本
- 2026-07-14｜两次多文件补丁因既有文档上下文不匹配而整体失败｜在未读取精确短文件和锚点前合并了多个修改｜拆分补丁并用已确认标题作为锚点后成功应用｜多文件补丁涉及既有文档时先读取精确上下文并分文件提交
- 2026-07-14｜Station GRU 正式训练被 1 秒工具超时关闭输出管道并写入 crash 行｜把工具让出控制误当成进程硬超时｜保留不可篡改 crash 行并以新实验名、完整超时重跑成功｜长训练使用可等待执行单元且禁止短硬超时
- 2026-07-14｜Station GRU 重跑描述中的分号被 Hydra override 语法拒绝｜把自然语言直接放入未转义 override｜删除非必要描述 override 后按原科学配置成功运行｜Hydra 自由文本使用配置文件或先验证引号转义
- 2026-07-14｜首次 held-out 评估路径含 `=` 被 Hydra 解析器拒绝｜PowerShell 引号未作为 Hydra 值的一部分保留｜在忽略目录复制同一 checkpoint 为 `best.ckpt` 后完成四模型评估｜CLI 评估统一使用不含 Hydra 特殊字符的 checkpoint 别名
- 2026-07-14｜新增真实消融套件首次 dry run 仍加载旧安装而拒绝 `real_lag_v1`｜修改源码后未先确认 `DeepWater` 中包的安装模式｜将当前仓库重新安装为 editable 并成功完成 dry run 与三种消融 smoke｜每次新增 CLI 能力后先核对 `RiverLagNet.__file__` 指向当前工作树
- 2026-07-14｜真实消融套件首个正式子进程在导入 site 时发生 UnicodeDecodeError｜编排器强制 `PYTHONUTF8=1`，与中文仓库路径的本地编码 editable `.pth` 冲突｜改为仅设置 `PYTHONIOENCODING=utf-8` 并清除子进程 UTF-8 模式｜Windows 中文路径下区分文件系统配置编码与标准流编码并增加环境回归测试
- 2026-07-14｜最终只读审计的 Python 单行命令被 PowerShell 截断并产生 SyntaxError｜在双引号命令中直接放入 Markdown 反引号，触发 PowerShell 转义｜改用不含反引号的独立审计命令并重新验证全部产物｜跨 shell 内联代码避免反引号等宿主 shell 特殊字符
- 2026-07-14｜查找汇总测试时 `rg` 把以 `--summarize` 开头的模式误判为选项｜未用 `--` 终止命令选项解析｜改用明确文件范围读取所需测试上下文｜搜索以连字符开头的模式统一使用 `rg -- '<pattern>'`
- 2026-07-14｜测试清理的首个多文件补丁因 `conftest.py` 导入锚点不存在而整体失败｜把待新增导入误写成既有上下文｜确认精确文件内容后拆分新增文件与修改文件补丁｜多文件补丁不得以尚未存在的代码作为匹配锚点
- 2026-07-14｜结果图首次保存时 Matplotlib 拒绝多色 `errorbar` 的端帽颜色列表｜误以为误差线 API 与 scatter 一样接受逐点颜色序列｜改为逐条件绘制误差线并保留统一点色｜多色误差线必须在目标 Matplotlib 版本上执行保存测试
- 2026-07-14｜河网诊断首次统计经纬度范围时 Polars 报重复投影列名｜同一列的 `min/max` 聚合未设置不同别名｜改为显式 `lon_min/lon_max/lat_min/lat_max` 后完成只读诊断｜同列多聚合在 Polars `select` 中必须逐项设置唯一 alias
- 2026-07-14｜README 与真实数据审计的合并补丁因审计句子锚点不一致而整体失败｜未先读取审计文档的精确旅行时间表述｜读取完整短文档后按实际段落拆分并成功应用｜跨生成报告与人工审计文档的补丁先逐文件确认锚点
- 2026-07-14｜清理旧绘图函数时两次自动生成的补丁因工具输出截断和 PowerShell 字符串序列化而失败｜一次读取过大的代码块且未显式把选择结果序列化为字符串数组｜改为分函数读取并使用 `ConvertTo-Json -InputObject` 后通过 `apply_patch` 完成清理｜自动构造补丁前先限制块大小并验证序列化类型
- 2026-07-14｜搜索图说明时引用了不存在的审计文档路径导致 `rg` 返回失败｜凭记忆写入文件名而未先列出目录｜用 `rg --files docs` 确认实际路径后继续检查｜不确定文档名时先枚举再搜索
- 2026-07-14｜追溯 HydroWQ 构图时误读不存在的 `hydrowq_catalog.py`｜按测试名猜测模块名而未先枚举源码｜定位到实际的 `hydrowq_import.py` 和上游构图模块后继续审计｜跨仓库追溯先用 `rg --files` 确认文件
- 2026-07-14｜读取完整样本清单产生超长截断输出｜未先判断清单规模且只需其中的构图元数据｜改为读取精简构建报告和构图源码的目标行｜大型 JSON 先查询键或使用精简审计文件
- 2026-07-14｜首次内联 Python 审计因中文仓库路径经过标准输入后无法导入 HydroWQ｜在 PowerShell here-string 中硬编码了中文路径｜改由环境变量传入解析后的路径并成功复现全部构图计数｜跨 shell 的非 ASCII 路径通过环境变量传递
- 2026-07-14｜构图诊断报告首次校验因图表来源缺少可执行 SQL 而失败｜只记录了 Python 审计说明，未满足报告组件的来源契约｜补充基于已审计计数的可执行 SQL 数据集并重新校验通过｜报告图表渲染前先确认来源字段满足验证器契约
- 2026-07-14｜再次假定 `conda` 在 PATH，并用递归目录扫描定位环境导致超时｜没有先读取已知的 `.conda/environments.txt`，且忽略了仓库中文路径与 editable `.pth` 的既有编码问题｜改用注册的 DeepWater 绝对解释器，并以 `python -S` 后追加 site-packages 的方式执行检查｜后续所有 Python 命令统一复用已验证的绝对解释器启动方式
- 2026-07-14｜新增测试与追加错误日志的合并补丁被整体拒绝｜终端乱码文本被误用作补丁上下文，且两个不相关修改共用一次补丁｜拆分补丁并以 UTF-8 读取的精确末行作为日志锚点后成功应用｜含既有非 ASCII 文档的补丁先显式按 UTF-8 读取，新增文件与文档修改分开执行
- 2026-07-14｜再次读取不存在的 `configs/experiment/real_lag_v1.yaml`｜把实验套件名称误当成 Hydra 配置文件名，重复了已记录的路径猜测错误｜枚举实际配置并从 `experiment_suite.py` 读取套件定义｜实验名、套件名和 Hydra 组名必须分别通过文件清单确认
- 2026-07-14｜大河网绘图分支与旧图脚注的合并补丁因乱码上下文被整体拒绝｜在已知终端默认解码不可靠时仍把显示乱码用于补丁锚点｜拆分补丁并只用稳定 ASCII 上下文完成分支接入｜非 ASCII 旧文本不参与组合补丁的匹配上下文
- 2026-07-14｜首次 contracted Lightning smoke 无法解析 `RiverLagNet.configs`｜包内 Hydra 配置目录缺少 `__init__.py`，源码模式回归测试未覆盖可导入性｜补齐配置包并增加 `import_module("RiverLagNet.configs")` 测试后 smoke 成功｜CLI 配置包既验证文件镜像也验证 Python 可导入性
- 2026-07-14｜训练就绪 MCP 报告前两次校验分别缺少 `dataset` 字段和图表内联 SQL 来源｜沿用了旧的 `datasetId` 习惯且误以为顶层 `sourceId` 足以满足图表来源契约｜改用 `dataset` 并补充基于审计计数的可执行 SQLite `UNION ALL` 查询后校验与渲染成功｜构建图表前从当前工具 schema 核对字段名和图表级来源要求
- 2026-07-14｜首版训练指标图的面板标题与副标题重叠｜标题和副标题都放在轴顶端默认位置，未在首次保存后预留垂直间距｜增加标题 pad、重新导出并目视确认无重叠｜所有正式图首次导出后检查标题、标签、图例和脚注碰撞
- 2026-07-14｜四个 contracted 正式训练行记录了不能代表当前工作树的旧 commit｜在完成收缩构图修改后先训练、后提交，违反了“提交代码后运行正式实验”的可复现顺序｜保留不可篡改的初始账本行，先提交并推送实现，再以同名实验和新运行目录重跑，汇总器选择带正确 commit 的最新行｜所有正式实验启动前先确认 `git status` 为空并记录 `git rev-parse HEAD`
- 2026-07-14｜Notebook 的 CRLF 被 `git diff --check` 判为尾随空白但提交命令仍继续执行｜旧版 PowerShell 的 `ErrorActionPreference` 没有把原生命令非零退出码转换为终止错误，且 nbconvert 默认写入了 CRLF｜不 amend 已有提交，机械转换 Notebook 为 UTF-8 LF 并创建后续修复提交｜关键 Git 门禁逐条执行并显式检查 `$LASTEXITCODE`，不与 `git commit` 放在同一命令单元
- 2026-07-14｜用 `rg` 直接读取含通配符的 Windows 运行目录时报路径语法错误｜把 PowerShell 通配符误当成 `rg` 会展开的文件系统模式｜改用 `Get-ChildItem -Filter` 枚举目录后再读取指标文件｜Windows 下先由 PowerShell 展开目录模式，不把未展开通配符传给 `rg`
- 2026-07-14｜再次把实验套件名误当成 `configs/experiment` 下的 Hydra 文件名｜没有先从 `experiment_suite.py` 确认 `real_contracted_lag_v2` 的定义位置｜改为读取套件注册表并确认其实际数据、训练器和条件覆盖｜实验套件始终先查注册表，只有训练命令明确引用 Hydra 组时才读取同名配置
- 2026-07-14｜首次 GPU 验证诊断被 1 秒命令超时提前终止｜误把嵌套 shell 的超时参数当成只控制输出让步｜将命令超时改为 120 秒并通过执行单元的 10 秒让步获得可等待任务，随后诊断成功｜长任务使用充足的命令超时，是否异步让步由执行单元参数控制
- 2026-07-14｜NSE 辅助损失的首个组合补丁因导入上下文不匹配被整体拒绝｜假定 Lightning 模块使用绝对导入，未先读取文件顶部的实际相对导入｜读取精确上下文后拆分为损失、模块、CLI 和配置四个小补丁并成功应用｜跨多个文件的补丁先逐一核对关键导入与配置锚点
- 2026-07-14｜读取训练测试时附带猜测了不存在的 `tests/cli/test_train.py`，导致命令以非零状态结束｜没有先枚举测试目录就拼接了第二个候选路径｜保留已成功读取的 checkpoint 测试并停止使用猜测路径｜访问测试文件前先用 `rg --files tests` 确认实际路径
- 2026-07-14｜两阶段 smoke 首次因 checkpoint 文件名中的 `=` 无法通过 Hydra 覆盖语法｜把带 `epoch=...-val_nse=...` 的路径直接作为未转义配置值｜对路径内两个等号使用 Hydra 反斜杠转义后真实 checkpoint 加载和单 batch 训练成功｜CLI 传递 Lightning checkpoint 路径时预先转义 Hydra 保留字符
- 2026-07-14｜核查静态 GAT 构造逻辑时读取了不存在的 `models/factory.py`｜未先确认 `build_model` 的实际定义位置便假定了常见工厂文件名｜改为从检索结果读取 `training/lightning_module.py` 并完成 checkpoint 结构核查｜先用 `rg -n "def build_model"` 定位定义，再读取返回的真实路径
- 2026-07-14｜接入1068节点数据配置前把不存在的 `tests/cli` 目录传给 `rg`｜未先枚举当前测试树便沿用常见目录假设｜改用 `rg --files tests` 定位真实测试与配置同步检查｜检索测试文件前先枚举，禁止猜测目录
- 2026-07-14｜1068节点轨迹传播 smoke 的 warm-start 覆盖被 Hydra lexer 拒绝｜虽转义 checkpoint 文件名中的等号，仍传入了含 Windows 盘符冒号的绝对路径｜改用仓库内相对正斜杠路径并保留等号转义后重跑｜Hydra checkpoint 覆盖统一使用相对 POSIX 风格路径
- 2026-07-14｜读取扩展数据 manifest 时触发本地默认编码错误｜未为含中文的 JSON 显式指定 UTF-8｜改为所有审计 JSON 读取均明确使用 UTF-8｜Windows 下读取项目文本不得依赖系统默认编码
- 2026-07-14｜再次猜测了两个不存在的实验配置文件名｜把运行名误当成 Hydra 配置名且未先枚举目录｜先列出 `configs/experiment` 后读取真实公共配置｜配置检查必须先枚举文件，运行名与配置组名分开核对
- 2026-07-14｜新增主干河道 Hydra 配置后首次全量测试缺少包内镜像｜只添加了根配置，遗漏仓库现有的根配置与 `src/RiverLagNet/configs` 双份同步约束｜补齐数据和实验配置镜像后重跑全量测试｜新增 Hydra 配置时同时写入根目录与包内目录
- 2026-07-14｜主干无图正式训练首次启动被过短的命令超时终止并记为 crash｜误把执行单元的让步时间当成 shell 命令超时，重复了已记录的长任务超时错误｜保留 crash 账本并用充足超时重跑得到正式结果｜训练命令固定使用覆盖完整训练的长超时，由执行单元负责阶段性让步
- 2026-07-14｜Station GRU 主干基线首次无法 warm-start RiverLagNet｜沿用此前 RiverLagNet-no-graph checkpoint 的兼容假设，未覆盖独立 Station GRU 缺少图分支参数的情况｜仅对明确识别为 station-only 的 checkpoint 允许缺少图分支并增加回归测试｜更换基线模型类型后先验证 checkpoint 参数契约再启动正式实验
- 2026-07-14｜输出输送模块首次 GPU mixed-precision smoke 因计数张量类型不一致失败｜单元测试只覆盖 float32，`torch.ones` 默认 float32 与 AMP 的 float16 累加目标冲突｜计数张量显式继承预测 dtype 并增加 float16 回归测试｜所有模型内新建浮点张量必须继承输入 device 与 dtype
- 2026-07-15｜多尺度路径功能的两次组合补丁因猜测既有上下文而失败｜没有先读取测试和架构文档的精确段落就合并多个文件修改｜读取真实上下文后拆成小补丁并成功应用｜修改既有测试或文档前先读取准确锚点，组合补丁只使用已核实上下文
- 2026-07-15｜正式实验完成后的首次进展更新误报了近似 NSE 数值｜在读取自动实验账本前根据 checkpoint 四位显示值估算了未验证的精确数｜立即读取 `experiments/results.tsv` 并更正为 0.5888225436｜正式数值更新必须先读取自动账本，不从截断显示反推精确值
- 2026-07-15｜目标条件化融合首次 GPU bf16 smoke 在 `index_add_` 发生类型冲突｜CPU 单测只覆盖 float32，目标头 softmax 和历史候选未显式继承预测 dtype｜将路由头权重及候选转换为预测 dtype 并增加 bf16 回归测试｜新融合模块必须在提交前同时覆盖 float32 单测与真实 AMP smoke
- 2026-07-15｜目标条件化融合第二次 GPU smoke 仍发生相同类型冲突｜首轮修复只转换目标头权重，且回归测试错误假设 attention 也为 bf16，遗漏 AMP 下稀疏 attention 保持 float32 的真实组合｜将目标权重聚合结果统一转换为预测 dtype，并用 float32 attention 加 bf16 预测回归｜混合精度测试必须复现模块边界两侧可能不同的实际 dtype，不能只测试全模块单一 dtype
- 2026-07-15｜目标条件化融合第三次 GPU smoke 仍在归一化累加处类型冲突｜第二轮修复仍让累加器继承预测 dtype，忽略 autocast 可让归一化求和保留 float32｜分子和分母累加器改为继承对应聚合 source dtype，聚合完成后才转换到解码 dtype｜`index_add_` 等原位归约的目标张量必须直接从 source 张量创建，不能从相邻模块输出推断 dtype
- 2026-07-15｜查找最新结果绘图入口时把不存在的 `scripts` 目录传给 `rg`｜没有先用 `rg --files` 确认仓库顶层目录就沿用常见项目结构假设｜改为只读取已枚举的 `src/RiverLagNet/analysis` 绘图模块｜检索命令的路径参数必须来自当前仓库文件清单，禁止附加猜测目录
- 2026-07-15｜读取主干基线训练参数时猜测了不存在的 `csv/version_0/hparams.yaml`｜没有先枚举实际 logger 版本目录，忽略首次 crash 使成功运行写入 `version_1`｜改为递归枚举 `hparams.yaml` 后定位真实文件｜运行目录内 logger 版本必须从文件清单读取，不固定假设 `version_0`
- 2026-07-15｜首次恢复 8 跳轨迹 checkpoint 时把 `max_lag` 设为当前默认 14 导致尺寸不匹配｜未先检查 checkpoint 中 31 行 lag embedding 所证明的 `max_lag=30`｜按 checkpoint 张量形状改为 30 并严格加载全部参数成功｜恢复历史 checkpoint 前先用状态张量形状核对不可从当前默认推断的结构参数
- 2026-07-15｜误差诊断首版的同验证集缩放 oracle 输出 NaN｜零上游修正位置的分母为零，而零岭惩罚分支直接计算了 `0/0`｜对零分母位置显式返回零缩放并重新生成诊断｜所有解析缩放和归约公式必须单测零信号分母
- 2026-07-15｜误差诊断 HTML 首次打包被表格默认排序契约拒绝｜`defaultSort.field` 使用了数据中的 `order`，但没有在表格列定义中声明｜补充显式顺序列后重新执行统一验证与打包｜报告表格的默认排序字段必须同时存在于快照数据和声明列
- 2026-07-15｜误差诊断 HTML 第二次打包因横向条形图轴字段契约失败｜按视觉方向把数值放在 `x`、类别放在 `y`，但共享图表契约统一要求类别 `x` 与数值 `y`，由类型控制方向｜交换 encoding 并把 15% 参考线改到数值轴｜原生图表 encoding 遵循共享语义轴契约，不从屏幕方向推断字段位置
- 2026-07-15｜误差诊断 HTML 第三次打包要求卡片来源提供 SQL｜把 Python 文件诊断错误声明为带 query 元数据的查询型 source，触发了 SQL 来源契约｜不伪造 SQL，改为真实仓库相对 JSON 文件路径来源｜非 SQL 分析产物必须使用文件型 source，不能为保留说明字段伪装成查询来源
- 2026-07-15｜改成文件型 source 后报告 metric cards 仍被打包器要求 SQL｜共享卡片校验当前不接受文件来源，即使报告来源是实际 JSON 诊断产物｜删除非必需 KPI 卡片，以技术摘要和原生图表承载相同指标，避免伪造 SQL｜非 SQL 技术报告不使用强制 SQL 来源的 metric-strip，先由叙事和文件来源图表表达
- 2026-07-15｜首次执行报告二级 SQL 时快照行序与 `ORDER BY rank` 不一致｜手工转录数值时保留了叙事顺序，没有同步查询排序语义｜将快照重排为 SQL 实际返回顺序并再次逐行相等校验｜声明 SQL 为快照来源时，字段、数值和行序都必须与实际执行结果一致
- 2026-07-15｜修复快照顺序时组合补丁误把错误日志锚点放进报告 JSON 文件｜将两个文件的修改合并后没有核对每个更新块的目标文件上下文｜拆分为报告快照与错误日志两个独立补丁后成功应用｜跨文件补丁必须逐块确认目标路径，日志追加不与数据文件修改共用上下文
- 2026-07-15｜误差诊断 HTML 首次通过结构校验后在 1440px 浏览器验证出现横向溢出｜来源 SQL 是超长单行，且 15% reference line 未被图表自动轴域纳入｜将 SQL 分行并把 15% 目标作为显式第八条基准数据，删除失效参考线后重跑桌面与窄屏 QA｜报告需同时检查隐藏来源面板的长文本宽度和基准是否真正进入可视轴域
- 2026-07-15｜报告 SQL 与快照核验脚本错误假设 `datasets` 位于制品顶层｜未先检查当前报告制品规范，实际快照位于 `snapshot.datasets`｜读取真实 JSON 层级后修正核验脚本并重跑｜对结构化制品执行二次校验前必须先枚举顶层及嵌套键
- 2026-07-15｜修正报告快照路径后又把 `snapshot.datasets` 假设为列表并执行切片｜只检查了路径，没有同时检查容器类型；实际为按数据集 ID 索引的字典｜改为打印类型和字典键后按 ID 读取｜检查嵌套结构时必须一次确认路径、容器类型和元素模式，避免连续猜测
- 2026-07-15｜便携报告首次局部修正粘性页头 `100vw` 后仍未通过横向溢出校验｜只处理了截图中最明显的页头，未覆盖增强阅读器内部其他亚像素溢出｜在保留表格内部滚动的前提下对文档根元素裁剪横向溢出，并再次运行官方验证器｜兼容性修复必须以完整验证结果为准，不能把单一可疑元素当作唯一根因
- 2026-07-15｜诊断提交前 `git diff --cached --check` 已报告生成 JSON 的 CRLF 尾空白但命令串仍继续提交并推送｜没有在 `diff --check` 后显式检查退出码，且生成脚本未固定 LF｜新增 JSON 的 LF 属性、让生成脚本显式使用 LF，并以新提交修复；后续提交命令在检查失败时立即退出｜任何提交前校验都必须作为硬门槛串联，不能只打印结果
- 2026-07-15｜递归时滞 attention 首次 CUDA bf16 测试在上下文赋值处发生 dtype 冲突｜为保持数值稳定让 sparsemax 权重保留 float32，但聚合时未转换为 bf16 候选值 dtype｜聚合前显式将权重转换为候选值 dtype，并保留真实 CUDA autocast 回归测试｜混合精度稀疏归一化应分离“权重计算 dtype”和“消息聚合 dtype”并在边界显式转换
- 2026-07-15｜递归时滞 attention 第二次 CUDA bf16 测试仍在同一赋值处冲突｜只转换了乘法输入，autocast 仍让归约结果回到 float32｜在归约完成后把最终消息再次显式转换为上下文 dtype，并同步覆盖多入边路径｜AMP 下不能从算子输入 dtype 推断归约输出 dtype，原位赋值前必须校验最终张量 dtype
- 2026-07-15｜RCELA 无图正式实验首次启动在 Hydra override 解析阶段失败｜`experiment.description` 含分号，超出未转义 override 字符语法｜确认训练尚未开始且未写结果，改用无标点下划线描述重新启动｜CLI 自由文本覆盖必须使用 Hydra 安全字符或配置文件承载，正式长任务前先做 override 解析检查
- 2026-07-15｜RCELA 无图正式训练完成 early stopping 后在 Rich 进度条 teardown 因 GBK 无法编码项目符号而记为 crash｜正式命令遗漏了已在 smoke 中使用的禁用进度条覆盖，Windows 旧控制台在结束渲染时触发编码错误｜保留自动 crash 账本和 checkpoint，以相同确定性配置在 `enable_progress_bar=false` 下重新训练并自动登记｜Windows 正式训练命令固定关闭 Rich 进度条，不能只在 smoke 命令中使用
- 2026-07-15｜为规避 Rich 输出编码而设置 `PYTHONUTF8=1` 后 Python 在导入 site 阶段失败｜DeepWater 环境存在本地编码的 `.pth`，全局 UTF-8 模式错误地改变了环境文件解码方式｜撤销全局 UTF-8，仅关闭 Rich 进度条并用 `PYTHONIOENCODING` 控制标准流｜现有 Conda 环境不能假设所有元数据均为 UTF-8，控制台编码修复不得改变文件系统默认解码
- 2026-07-15｜纯 innovation 正式训练的执行单元在 3600 秒超时后误判 Python 训练进程已终止｜只看到外层 PowerShell 超时，未先检查其 DeepWater 子进程仍在 GPU 上运行｜核对 PID、GPU 与 metrics 后取消恢复计划，仅轮询原进程直至其正常 early stopping 和自动记账｜长训练超时必须高于最坏 early-stopping 时长，外层超时后先查子进程再判断是否恢复，禁止直接重复启动
- 2026-07-15｜DCUV 首次组合补丁因架构文档锚点与实际文本不一致而整体未应用｜依据压缩摘要猜测文档原句，没有先读取对应段落｜读取真实上下文后拆分代码与文档补丁并重新应用｜跨代码和文档的大补丁必须先核对每个文件的精确锚点，避免一个非关键锚点回滚全部修改
- 2026-07-15｜DCUV 真实数据 fast-dev 首次启动在 Hydra 解析绝对 `run_dir` 时失败｜把含 Windows 盘符和中文目录的未转义绝对路径直接作为 override 值｜确认训练未开始且测试目录已清理，改用仓库内相对运行目录重试｜Hydra 路径覆盖优先使用相对路径，正式启动前避免把带冒号的 Windows 绝对路径作为裸值
- 2026-07-15｜DCUV checkpoint 参数核验命令使用了不存在的诊断脚本文件名并返回失败｜沿用了概括性名称而未先枚举 analysis 目录，实际入口是 `mainstem_error_diagnostic.py`｜用 `rg --files` 找到真实入口后继续，并扩展该入口支持 adaptive value mode｜调用项目脚本前先按文件清单确认路径，不能依据功能描述猜文件名
- 2026-07-15｜首次环境审计在仓库父目录调用 Git 且假设 `conda` 已在 PATH｜没有先确认仓库根目录与 Conda 可执行文件位置，导致并行审计命令部分失败和超时｜进入 `RiverLagNet` 根目录并定位到 `C:\Program Files\ANACONDA\Scripts\conda.exe` 后继续｜环境审计先解析仓库根目录和解释器绝对路径，再并行执行依赖命令
- 2026-07-15｜动态传播时间 smoke 首次通过 Conda 捕获输出时在回显阶段触发 GBK 编码错误｜只修正了子进程编码，遗漏 Conda 25.5 仍会用父控制台编码重放捕获内容｜改用 `conda run --no-capture-output` 并保留子进程 `PYTHONUTF8=0`｜Windows Conda 长任务统一禁用捕获输出，避免训练成功与包装器回显失败混淆
- 2026-07-15｜HCTS 架构文档首次组合补丁因段落锚点未匹配而整体未应用｜把视觉输出中的换行直接作为跨文件补丁上下文，没有先用最小稳定标题锚点｜拆成按标题和单句定位的两个补丁后应用｜多文档补丁优先使用唯一标题或最小稳定上下文，并在组合前分别核对
- 2026-07-15｜双阶段模型配置首次由字符串拼装补丁缺少合法结尾｜自动拼接时把文件尾换行错误转换为补丁内容，导致 `End Patch` 不再是最后一行｜改为显式补丁并成功创建两份配置｜新增短配置文件直接使用显式补丁，避免为少量重复行引入脆弱拼装
- 2026-07-15｜双阶段直接边路由首次真实 bf16 smoke 在 `index_add_` 发生 dtype 冲突｜CPU 测试未暴露 scatter softmax 的 float32 权重与 bf16 消息聚合边界｜聚合前显式转换权重与最终消息 dtype，并新增真实 CUDA autocast 回归测试｜新增稀疏聚合算子必须同时覆盖 CPU 契约和目标混合精度设备
- 2026-07-15｜四会话整合的首次跨文件组合补丁因上下文顺序不匹配而整体未应用｜同一补丁中的 AGENTS hunk 没有按文件出现顺序排列｜改为按文件和行序拆分小补丁并完成更新｜大型跨文件补丁必须按文件内行序组织，失败后先核对真实上下文再重试
- 2026-07-15｜HCTS 正式筛选附着于交互工具会话并在用户消息到达时被宿主终止｜长训练使用了前台等待单元，未隔离会话中断；运行停在 epoch 4 且未形成正式结果记录｜保留临时 checkpoint 但不冒充完整结果，后续正式训练改为隐藏后台进程、独立 stdout/stderr 与指标账本｜超过一分钟的正式训练必须后台化并通过进程和文件轮询监控
- 2026-07-15｜环境上下文刷新后继续调用已不可用的延迟 `shell_command` 工具｜沿用刷新前的工具别名，没有重新以当前显式工具表为准｜切换到稳定公开的 `exec_command`｜上下文或权限刷新后重新核对可调用工具，不复用可能消失的延迟别名
- 2026-07-15｜实施计划接口检索首次在仓库父目录执行｜只依据会话工作目录，未先确认 Git 仓库根目录，导致 `rg` 报告源码路径不存在｜定位到 `Code/RiverLagNet` 后重新执行检索｜任何仓库命令前先用目录清单或 `git rev-parse --show-toplevel` 确认根目录
- 2026-07-15｜实施计划自审首次把通配符直接作为 Windows `rg` 路径参数｜沿用 shell 展开通配符的假设，但 PowerShell 未替 `rg` 展开该路径｜改用固定目录和 `-g` 文件过滤重新检查｜Windows 下调用 `rg` 时用 `-g` 表达文件模式，不依赖 shell 路径通配展开
- 2026-07-15｜实施计划交付的两次完整测试通过 `conda run` 均在 site 导入前遇到 `.pth` UTF-8 解码失败｜首次遗漏已知约束，第二次又错误假设进程级 `PYTHONUTF8=0` 足以覆盖 Conda 包装层｜改用 DeepWater 解释器绝对路径和 `-X utf8=0`，确认 `cp936` 后重跑并通过 195 项测试｜本机验证优先使用环境解释器绝对路径与显式 Python 启动参数，不依赖 Conda 对编码环境变量的转发
- 2026-07-15｜用 `fast_dev_run` 后的 `Trainer.max_steps` 断言预算配置传播，导致测试错误失败｜忽略了 Lightning 会把 fast-dev 的公开步数强制重写为 1｜改用非 fast-dev 的 2-step 短运行验证预算传播｜后续不得用 fast-dev 模式断言用户配置的训练上限
- 2026-07-15｜把预算传播测试改成 epoch 内提前停止的非 fast-dev 运行，触发缺少验证指标的 strict EarlyStopping｜未考虑 `max_steps` 可在首次 validation 前终止｜改为断言 CLI 返回的已解析预算元数据并保留 fast-dev 链路 smoke｜短预算测试不得依赖完整 epoch/validation 控制流
- 2026-07-15｜RuntimeStats 把含设备名和 matmul 策略的完整硬件字典传给 TensorBoard scalar logger｜混淆了机器可读 JSON 元数据与数值指标接口｜logger 仅接收数值字段，完整字典只写硬件 JSON｜新增运行元数据时必须区分标量日志与结构化产物
- 2026-07-15｜首次 Blackwell smoke 在 BF16 fused AdamW 与 Trainer 梯度裁剪组合上失败｜未验证 Lightning 2.6 会把无 GradScaler 的 BF16 fused optimizer 也视为内部 unscale｜在 module hook 中仅对无 scaler 的 fused 路径手动裁剪未缩放梯度并增加真实 CUDA 回归测试｜正式 GPU 配置必须用真实 optimizer/precision/clipping 组合做 smoke
- 2026-07-15｜第二次硬件 smoke 完成训练后在 CUDA 峰值收尾时报 module 位于 CPU｜测量回调在 Lightning teardown 后仍动态读取 `pl_module.device`｜计时开始时冻结 CUDA device 并用于全部同步和峰值查询｜跨 teardown 的回调不得依赖 module 当前设备
- 2026-07-15｜首次完整矩阵的 batch 24 跨 epoch 后因禁用 validation 却保留 `ReduceLROnPlateau(val_macro_nse)` 而中断｜复用正式 module 时未隔离依赖验证指标的 scheduler｜为无指标硬件基准显式禁用 scheduler、保留相同 AdamW，并增加配置测试｜关闭验证的吞吐任务必须同步关闭所有验证指标消费者
- 2026-07-15｜论文文档首次自动核对脚本对 Decimal 调用不存在的 `Substring` 方法并提前退出｜加入了无实际用途且未经核对的类型转换检查｜删除该检查并重新运行同一组文档、实验数值和 Git 差异校验｜验证脚本只保留直接断言，新增类型操作前先确认对象类型
- 2026-07-15｜论文文档第二次自动核对把清单整数 `3972` 与正文千分位格式 `3,972` 直接比较并误报缺失｜验证逻辑忽略了面向读者的数字格式化｜按千分位格式生成预期文本后重新核对｜机器记录与成稿互查时先统一数值格式，不用原始字符串直接比较
- 2026-07-15｜首轮硬件矩阵用 accumulation=1 比较不同 physical batch，导致 optimizer steps/s 对应不同样本曝光｜只实现了 Task 6 的物理批量网格，遗漏设计中“物理批量不同时以梯度累积保持有效批量”的公平性约束｜以候选批量最小公倍数 96 固定 effective batch，并把 accumulation 和有效批量写入每条测量及测试｜吞吐矩阵在开始长跑前必须断言所有候选的每更新样本曝光一致
- 2026-07-16｜等曝光硬件矩阵被会话消息中断后丢失已完成候选｜基准器只在全流程结束时写最终 JSON，没有逐候选持久化｜增加带协议签名、逐条 fsync 的 JSONL journal 和自动续跑测试，最终产物成功后才删除 journal｜任何超过单个候选时长的矩阵任务必须先实现原子进度保存与恢复
- 2026-07-16｜top-2 三次重复后选择器把仅测一次的第三名重新选为赢家并错误启动 compile｜最终排名仍混入所有首轮候选，没有要求同等重复次数｜最终选择增加 `minimum_repetitions=3` 门槛并用失败测试覆盖未重复的更快候选｜重复测量协议必须把重复数作为候选资格条件，而不只计算已有样本的中位数
- 2026-07-16｜Session A synthetic GPU fast-dev 训练结束后 RuntimeStatsCallback 用 CPU 设备查询 CUDA 峰值而退出失败｜Lightning teardown 已把 module 移回 CPU，回调仍动态读取 `pl_module.device`｜在 fit 开始时冻结实际 CUDA device，并增加真实 CUDA teardown 回归测试｜跨 teardown 的硬件回调不得依赖 module 的结束时设备
- 2026-07-16｜复跑 CUDA 回归测试的首次工具调用因 JavaScript 工作目录字符串中的反斜杠被解析为八进制转义而未执行｜Windows 路径没有使用原始字符串或双重转义｜改用 `String.raw` 保存工作目录并重新执行同一测试｜在 JavaScript 工具编排中统一用原始字符串表达 Windows 路径
- 2026-07-16｜Session A 交付审计的首次并行工具调用因嵌套 PowerShell 正则引号破坏 JavaScript 字符串而未执行｜混用了 JavaScript 单引号和 PowerShell 双单引号转义｜把包含正则的命令整体改为 `String.raw` 模板后重新执行｜嵌套 shell 命令优先使用原始模板字符串，不跨语言复用引号转义规则
- 2026-07-16｜Session B 接管审计的首次并行工具调用因内嵌 Python 字符串引号破坏 JavaScript 源码而未执行｜在普通 JavaScript 单引号字符串中嵌套了 PowerShell 与 Python 的多层单引号｜将各命令统一改为 `String.raw` 模板并简化 Python 输出拼接后重跑｜三层语言嵌套命令必须从最外层使用原始模板字符串并避免复用同类引号
- 2026-07-17｜Task 3 接口审计读取了不存在的 `tests/models/test_models.py`｜未先用文件清单确认模型测试文件名｜改读已存在的 `test_baselines.py` 并列出 `tests/models`｜读取测试前先用 `rg --files` 获取权威路径
- 2026-07-17｜Task 3 首次 `rg` 在 Windows 上把 `src/RiverLagNet/models/*.py` 作为路径传入而报错｜再次依赖 shell 展开通配符而 PowerShell 未替 `rg` 展开｜改用固定目录加 `-g "*.py"`｜Windows 下所有 `rg` 文件模式统一通过 `-g` 传递
- 2026-07-17｜Task 4 首版模型注册测试只向公共模型传入了 `x` 一个参数｜编写断言时绕过了统一 Lightning batch 接口｜在运行红测前自审并改用 `RiverForecastModule(model)(batch)` 验证完整公共合同｜新增模型集成测试必须经过与训练入口相同的 batch 适配层
